import React from 'react';
import Button from './Button';

const Login = () => {
    return (
        <div>
        <h1>Login Page</h1>
        <Button page={"/"} text={"Back"}></Button>
        </div>
    );
};

export default Login;