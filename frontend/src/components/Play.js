import React from 'react';
import Button from './PageButton';

const Play = () => {
    return (
        <div>
        <h1>Play</h1>
        <Button page={"/"} text={"Back"}></Button>
        </div>
    );
};

export default Play;