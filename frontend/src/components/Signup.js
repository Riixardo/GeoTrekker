import React from 'react';
import Button from './Button';

const Signup = () => {
    return (
        <div>
        <h1>Signup</h1>
        <Button page={"/"} text={"Back"}></Button>
        </div>
    );
};

export default Signup;